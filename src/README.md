# Source Workspace

Active projects:

- `FnMappingTool.Core`
- `FnMappingTool.Worker`
- `FnMappingTool.Controller`

Use the repo-root build script:

```powershell
..\build.ps1
```

Architecture notes:

- `..\AGENTS.md`

Portable output:

- `..\portable\FnMappingTool\`
